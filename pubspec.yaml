name: tests_in_flutter
description: Tests in Flutter.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  cupertino_icons: ^1.0.8
  extended_masked_text: ^3.0.1
  flutter:
    sdk: flutter
  intl: ^0.19.0

dev_dependencies:
  flutter_driver:
    sdk: flutter
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
